{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "execute python in docker",
            "type": "shell",
            "command": "docker exec -it 'python-flask' python ${fileDirname} "
        }
    ]
}